//  compute the sum of the elements of an array and display it in the console

// asta nu merge

// function firstArray() {    
//     var myArr = [1, 2, 3, 4, 5];
//     var sum = 0;
//     for (i = 0; i <= myArr.lenght - 1; i++) {
//         sum += myArr[i];
//     }
//     console.log("Sum is " + sum);
// }
// firstArray();

// asta merge

function secondArray() {
    var array = [1, 2, 3, 4, 5];
    var sum2 = 0;
    for (i = 0; i <= array.length - 1; i++) {
        sum2 += array[i];
    }
    console.log("Sum is " + sum2);
}
secondArray();


// nu gasesc nici o diferenta intre cele doua variante, totusi una merge si una nu merge